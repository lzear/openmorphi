mutation createAnimation (
    $hexcode1: String!
    $hexcode2: String!
    $html: String!
) {
    createAnimation (
        data: {hexcode1: $hexcode1, hexcode2: $hexcode2, html: $html}
    ) {
        _id
        hexcode1
        hexcode2
        _ts
    }
}
