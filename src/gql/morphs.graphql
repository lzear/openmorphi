query morphs (
$size: Int
$cursor: String
) {
    morphs (
        _size: $size
        _cursor: $cursor
    ) {
        data {
            _id
            hexcode1
            hexcode2
            data
            _ts
        }
        before
        after
    }
}